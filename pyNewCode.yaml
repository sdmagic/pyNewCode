project: "MyNewProject"
version: "0.0.1"
author: "Stephen D. Cooper"

# The Project's directories
# NEVER include the drive: in these directories
directories:
  config: "config"
  logs: "logs"
  modules: "modules"

# Options for the application
options:
  logging: true
  screenpretty: true
  screenprint: true

# Project Files to build (write)
build:
  init: True
  main: True
  config: True
  yaml: True
